import Modal from "./Modal"

const Cart = (props) => {
    return (
    <Modal toggleCart={props.toggleCart}>
        <div>
        <h1>WTF</h1>
        <h3>Total</h3>
        </div>
    </Modal>
    )
}

export default Cart